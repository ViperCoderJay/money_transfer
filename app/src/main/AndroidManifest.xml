<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tech.jamesnjovu.supple">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MoMo.Transfer.Transfer"></activity>
        <activity android:name=".MoMo.TopUp.TUzamtel" />
        <activity android:name=".MoMo.TopUp.TUmtn" />
        <activity android:name=".MoMo.TopUp.TopUp" />
        <activity android:name=".MoMo.TopUp.TUairtel" />
        <activity android:name=".MoMo.TopUp" />
        <activity
            android:name=".user.Register"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".user.EnterDetails" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>